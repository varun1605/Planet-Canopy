name: My first workflow

on: push

jobs:
    first-job:
      runs-on: ubuntu-latest
      steps: 
      
      - name: Welcome message
        run: echo "My first github action"

      - name: List the files 
        run: ls
      
      - name: Read the file 
        run: cat README.md


